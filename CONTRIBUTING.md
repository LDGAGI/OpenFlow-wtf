# 参与 OpenFlow

感谢你愿意帮助 OpenFlow 变得更好。无论是报告问题、改进文档、修复错误，还是增加通用的 API 适配，我们都欢迎。

## 提交问题

提交 Issue 前，请先搜索是否已经有人报告过同样的问题。请尽量说明：

- 你原本想完成什么。
- 实际发生了什么。
- 可以稳定复现的操作步骤。
- 使用的系统、浏览器和 Node.js 版本。
- 不含密钥和私人地址的错误信息。

不要在 Issue、截图或日志中公开 API Key、Cookie、账号、用户素材。

## 提交代码

1. Fork 仓库，并从最新代码创建一个范围清晰的分支。
2. 只修改与当前问题有关的内容，避免混入无关重构。
3. 保持现有代码风格，并为行为变化补充必要测试。
4. 在本地完成检查后提交 Pull Request。
5. 在 Pull Request 中说明修改原因、主要行为和验证结果。

提交前请运行：

```bash
npm run typecheck
npm run lint
npm test
npm run build
```

如果某项检查因环境限制无法运行，请在 Pull Request 中如实说明。

## API 适配原则

OpenFlow 是一个允许用户接入自有 API 的通用基座。新增适配时，请遵守这些原则：

- 不得提交真实 API Key、私人中转站域名、账号或计费信息。
- 不得把某个贡献者的模型清单和个性化配置设为项目默认值。
- 上游模型 ID 应原样传递，不能仅因名称未知就拒绝保存或调用。
- 优先扩展通用协议适配器，避免把供应商特例散落在界面组件中。
- 测试使用 `provider.example`、`test-key` 等明显虚构的数据。
- 图片和视频生成可能产生费用，自动化测试不得调用真实付费接口。

需要接入新的 API 时，可以先按照 [PROVIDER_SETUP.md](PROVIDER_SETUP.md) 整理文档和适配要求。

## 界面修改

界面样式使用 CSS Modules。请检查桌面和移动视口，确保文字、按钮和菜单没有遮挡，并确认浏览器控制台没有新增错误。涉及用户操作变化时，请在 Pull Request 中附上截图或简短录屏。

## 贡献许可

OpenFlow 使用 [MIT License](LICENSE)。向本项目提交代码、文档或其他内容，即表示你确认自己有权提交这些内容，并同意按照 MIT License 将贡献授权给项目使用和分发。

项目目前不要求签署 CLA。维护者可能要求修改不符合项目范围、安全要求或质量标准的提交，也可能拒绝包含私人配置、未经授权代码或敏感信息的贡献。
